model Organization {
  id        String @id @default(cuid())
  name      String @unique
  openTime  String
  closeTime String

  courtLimit Int
  courts     Court[]
  users      User[]

  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
}
